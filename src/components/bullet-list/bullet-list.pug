include ../text/text

//-
  options.points - массив текстов для пунктов списка
mixin bullet-list(options = {})
  - const { points = [] } = options;

  ul.bullet-list
    each point in points
      li.bullet-list__point
        +text({ text: point })
