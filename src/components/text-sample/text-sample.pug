mixin text-sample(options = {})
  -
    const {
      theme = 'title',
      tag = '',
      text = ''
    } = options;
    const textTheme = `text-sample_theme_${theme}`;

  .text-sample(class = textTheme)
    .text-sample__tag= tag
    .text-sample__text= text
